// Aula 4 - Conhecendo o ambiente de desenvolvimento: Node e JS 

// Númerico

let idade = 35;
let altura = 1.60; // não utilizamos "," para separar os numero, usamos o  "."

// Texto (string)

let nome = "Joao";

// Booleanos

let choveuHoje = true;

let falteiNaAula = false;

// Null = valor vazio ou desconhecido.

let temperatura = null;

// Undefined (Sem valor definido)

let saudade;

saudade = "Oi, sumido" // agora foi colocado o valor na variavel que estava sem valor.

//Criação de uma objeto literal

var carro = {
    ano : 2016,
    modelo : "onix"
}

console.log (carro);

// Array

let series = ["Insecure","SexEducation", "Genera+ion"];

console.log(series);

